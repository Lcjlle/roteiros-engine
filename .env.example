# Postgres. `docker compose up -d db` before anything touches this.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/roteiros

# Chave de API do provedor usado por `instructor` nas Fases 4/5 (anotacao em
# lote) e 7 (geracao). Preenchida quando a Fase 5 comecar - nao antes.
# OPENAI_API_KEY=
